# Fallback for a pnpm 10.x release older than 10.17 (before pnpm-workspace.yaml grew its own settings block); ignored on pnpm 11+, which reads these from pnpm-workspace.yaml instead.
save-exact=true
minimum-release-age-exclude[]=@exadev/eslint-config
